git init  
git config --global user.name "IrfanKondakamarla"
git config --global user.email "irfankondakamarla@gmail.com"

git config --list > To check properly configured or not 
git log - To anyalyze the Files in Local respository 
git add <file name> --> it adds the file to staging/Index Area from workspace
git commit -m "label" file-name  ---> Moves the file to INdex Area
git show <commit id> ---> shows the name of the file in local respository

git add * or git add . or git add -A or git add <file1> <file2> ..... <filen>
git reset head <file name> ---> File can be reverted back to workspace from staging/Index Area
git reset --soft <commit ID> ---> Commit ID should be given n-1, Mean lower the head commit ID
git reset --mixed <Commit ID> ---> Reset the file from Local repository to Workspace *Also it moves all data of staging/Index to workspace
git reset --hard <commit ID> ---> it deleted the files from Local Repository as well from all stages
git clone <central repository url : https://github.com/ayaanaahil/repo1.git > This will clone the Repo1 created in GIT HUB over cloud 
goto central repository in your system --> add files in similar process like above and use below command to push file directly to central repository 
git push 
